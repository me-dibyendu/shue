import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

const Orders = () => {
 
  return (
    <View>
	
	</View>
  );
};

const styles = StyleSheet.create({
  
});

export default Orders;
